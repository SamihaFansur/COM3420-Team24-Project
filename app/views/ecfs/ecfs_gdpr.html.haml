
-if @ecfs.empty?
    .alert.alert-primary No ECF found ! Please try again.

-if  ! @ecfs.empty?

    .alert.alert-warning{:role => "alert"}
        Ecfs displayed here have been in the system for 7 years and need to be deleted as per the
        = succeed "." do
            %a.alert-link{:href => "https://www.gov.uk/data-protection"} GDPR Policy.
    .display-5.mt-5.mb-5
        Todays Date: #{Date.today}

    %p
        - @ecfs.each do |ecf|
            - if Time.zone.now.years_ago(7) >= ecf.created_at
                %table.table
                    %thead
                        %tr
                            %th{:scope => "col"} Status
                            %th{:scope => "col"} Date
                            %th{:scope => "col"} First Name
                            %th{:scope => "col"} Email
                            %th{:scope => "col"} User ID
                            %th{:scope => "col"} Module
                            %th{:scope => "col"} Show
                            %th{:scope => "col"} Delete
                    %tbody
                        %tr
                            %th{:scope => "row"}
                                = ecf.status
                            %td
                                = ecf.date
                            %td
                                = ecf.user.givenname
                            %td
                                = ecf.user.mail
                            %td
                                = ecf.user.uid
                            %td
                                = ecf.user.ou
                            %td
                                = link_to 'Show', ecf
                            %td
                                = link_to 'Delete ECF', ecf_path(ecf), :method => :delete, :data => { :confirm => 'Are you sure?' }
