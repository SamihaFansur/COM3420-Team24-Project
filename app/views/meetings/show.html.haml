%p#notice= notice

%p
  %b Title:
  = @meeting.title
%p
  %b Time:
  = @meeting.time

%p
  %b Attendees:
  = @meeting.attendees

%p
  ECFs on the agenda are listed below. To import an ECF into this meeting, please select 'Add to meeting' for an ECF at the following page:
  = link_to 'List ECFs', ecfs_path(meeting_id: @meeting.id)

%table.table
  %thead
    %tr
      %th{:scope => "col"} Status
      %th{:scope => "col"} Date Submitted
      %th{:scope => "col"} Full Name
      %th{:scope => "col"} Email
      %th{:scope => "col"} Username
      %th{:scope => "col"} Department
      %th{:scope => "col"} Panel notes
      %th{:scope => "col"} Outcomes
  %tbody
    - @agendas.each do |agenda|
      %tr
        %td= agenda.ecf.status
        %td= agenda.ecf.date
        %td #{agenda.ecf.user.givenname} #{agenda.ecf.user.sn}
        %td= agenda.ecf.user.mail
        %td= agenda.ecf.user.uid
        %td= agenda.ecf.user.ou
        %td
        %td
          - agenda.decisions.each do |decision|
            %b= decision.module_code
            = decision.assessment_type
            %b{ style: 'color: dodgerblue;'}= decision.requested_action
            = ": #{decision.outcome}"
            %br
        %td= link_to 'Show', agenda.ecf
        %td= link_to 'Edit', edit_ecf_path(agenda.ecf)

= link_to 'Edit', edit_meeting_path(@meeting)
\|
= link_to 'Back', meetings_path
